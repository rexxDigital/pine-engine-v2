#version 330 core

out vec4 outputColor;

in vec2 uvCoords;

uniform sampler2D frameBuffer;

void main()
{
    vec4 color = texture(frameBuffer, uvCoords);

    float gamma = 1.2f;

    color.rgb = pow(color.rgb, vec3(1.0/gamma));

    outputColor = color;
}